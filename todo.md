# TODO

## 優先度:高

- ~~月の英名（3文字の略称）を表示する。~~
- ~~曜日の英名（3文字の略称）を表示する。~~
- ~~スリープ状態に遷移しないようにする。~~
- ~~画面の任意の位置をタップすることで全画面化するのではなくフルスクリーンボタンを設ける。~~

## 優先度:中

- ~~「スリープ状態に遷移しない」条件として、「全画面状態」かつ「充電中またはUSB給電中」のみとする。~~
- 照度センサーの値によって、文字色や背景色を変更する。（まずは「平常」（現状）と「やや暗い」の２段階とする。照度センサー未実装の場合は「平常」のみとする。） 
- 縦長表示に対応する。（画面の縦横比率が縦長の場合、1行めに「時刻」、2行めに「年月日」を表示）
- オフライン状態、またはAPIキーやロケーションが異常だった等の様々な原因で WeatherAPIから値が取得できなかった場合、天候の表示を「----」にする。
- 設定ボタン及び設定画面の実装
    - 設定画面には「保存」「閉じる」ボタンがある。
    - ひとまず設定項目はない。
    - プロジェクトのクレジット（プロジェクトリポジトリのURL）と、WeatherAPI.comのクレジットを表示する。（これらのリンク はPWA内で開かず、別途ブラウザで開く）（WeatherAPI.comのクレジットは公式ドキュメントにしたがって以下のようにする）
        - Powered by <a href="https://www.weatherapi.com/" title="Weather API">WeatherAPI.com</a>

## 優先度:低

- 設定画面の充実。
    - 設定画面から WeatherAPI.com の APIキーを設定できる。（設定値はローカルストレージに保存する）
    - 設定画面から天気のロケーションを設定できる。（設定値はローカルストレージに保存する）
    - 設定項目「APIキー」「ロケーション」の横に、現在の設定値を表示する。
- 初回起動時に「ホーム画面に追加しますか？」のプロンプトを出す。
- APIキー、またはロケーションがローカルストレージに設定されていない（未設定または空文字だった）場合、設定画面を自動的に表示し、設定を促す。
- 設定項目「液晶タイプ」を「有機EL」「液晶」で切り替えられるようにする。
    - 有機ELの場合、表示位置を定期的にずらすことで焼き付きを防止する。
    - 液晶の場合、表示位置は固定。
- 秒針の代わりに、１秒毎に点滅するインジケーターを実装する。
- トランザクション数を節約するために、無為に WeatherAPI.com から値を取得し続けないようにする。現在１時間に１回、かつ、起動時に取得しているが、起動時にも無駄に取得しないようにする。具体的には、取得した後には必ずローカルストレージに取得日時を保持しておき、そこから 30分以上経過していない場合は WeatherAPI.com から値を取得しない。

